import React from 'react';
import styles from './Styles';

const Section = () => {
    const {Section} = styles;
    return ( 
        <Section>
            DESDE SECTION
        </Section>
     );
}
 
export default Section;